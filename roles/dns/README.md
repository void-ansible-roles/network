# dns

This role configures DNS related items on Void Linux including the
`/etc/hostname` file, the values in `/etc/hosts`, and optionally a
list of static IPv4 and IPv6 resolvers in `/etc/resolv.conf`.
